import React, { useEffect } from 'react'
//import { useSearchState , SearchState } from "../lib/store.ts";

const ResultDisplayController:React.FC = () => {
    //Destructure searchState global state

    //const { searchState, searchStateSetter }:SearchState = useSearchState() 

    useEffect(() => {

    },[])

    return (
        <div>
        </div>
    )
}

export default ResultDisplayController